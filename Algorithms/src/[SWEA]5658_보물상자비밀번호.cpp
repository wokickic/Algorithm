#include <iostream>
#include <vector>
#include <deque>
#include <string>
using namespace std;

int n, k;

int main(void) {
	int T; cin >> T;
	for (int t = 1; t <= T; t++) {
		cin >> n >> k;
		int rotate = n / 4;
		string s;
		cin >> s;
		printf("%c\n", &s);
		cout << s << "\n";
		/*for (int i = 0; i < n; i++) {
			
		}*/
	}
}